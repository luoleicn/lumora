{
  "name": "lumora",
  "version": "0.1.0",
  "private": true,
  "description": "lumora — light up your literature. Local-first desktop research literature manager.",
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @lumora/desktop",
    "dev:desktop": "npm run dev --workspace @lumora/desktop",
    "build": "npm run build --workspace @lumora/shared && npm run build --workspace @lumora/desktop",
    "typecheck": "npm run build --workspace @lumora/shared && npm run typecheck --workspace @lumora/desktop",
    "test": "npm run test --workspace @lumora/shared && npm run test --workspace @lumora/desktop --if-present",
    "test:coverage": "npm run test:coverage --workspace @lumora/desktop"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@types/node": "^24.0.10",
    "@vitest/coverage-v8": "^3.2.4",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
